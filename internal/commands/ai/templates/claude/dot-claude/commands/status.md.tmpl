---
description: Offline drift report — Wire/Swagger staleness, pending migrations, uncommitted generated files, go.sum freshness.
allowed-tools: Bash(gofasta status*)
---

Run `gofasta status --json` and report each check's status. If any
check reports `drift` or `pending`, surface the remediation hint from
its JSON payload and ask whether to fix before continuing.
