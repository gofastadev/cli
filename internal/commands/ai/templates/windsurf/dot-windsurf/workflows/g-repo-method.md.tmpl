# G repo-method

Add a method to an existing repository interface + implementation.

1. Ask the user for `<Resource> <MethodName> [param:type ...]`.
2. Run `gofasta g repo-method <args> --dry-run --json` to preview.
3. Show the user the planned edits.
4. Re-run without `--dry-run` to apply if approved.
