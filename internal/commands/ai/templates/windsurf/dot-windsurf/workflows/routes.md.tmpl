# Routes

List every registered REST route.

1. Run `gofasta routes --json`.
2. Group the result by registering file.
3. Flag duplicates, unusual method/path combinations, or routes that
   bypass middleware their neighbors apply.
4. Useful before adding middleware, changing auth, or hunting a 404.
