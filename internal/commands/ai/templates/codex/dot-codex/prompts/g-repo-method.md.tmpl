---
description: Add a method to an existing repository interface + implementation (modify-aware generator).
argument-hint: <Resource> <MethodName> [param:type ...]
---
Run `gofasta g repo-method $ARGUMENTS --dry-run --json` first to
preview the planned edits to the repository interface and
implementation. Show the result, then re-run without `--dry-run`
to apply if approved.
