# G-rename — rename a field across model, DTOs, service, tests

Run `gofasta g rename <Resource>.<OldField> <NewField> --json`
(preview mode) first. Show every file the generator would patch and
the migration pair it would emit.

Only re-run with `--apply` after the user confirms.
