# Inspect-tasks — list registered async tasks

Run `gofasta inspect-tasks --json` (or `gofasta inspect-tasks
<name> --json` for one task). Static AST scan of `app/tasks/`.

Report each task's wire name, payload struct, and whether handler +
enqueue functions are present. Use before adding a new task to avoid
name collisions.
