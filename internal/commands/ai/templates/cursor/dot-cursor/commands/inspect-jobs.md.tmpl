# Inspect-jobs — list registered cron jobs

Run `gofasta inspect-jobs --json` (or `gofasta inspect-jobs <name>
--json` for one job). Static AST scan of `app/jobs/`.

Report each job's name, type, schedule (from config.yaml), and
source file. Use before adding a new job to avoid name collisions
or schedule overlaps.
