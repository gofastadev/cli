# G-repo-method — add a method to a repository

Run `gofasta g repo-method <Resource> <MethodName> [param:type ...]
--dry-run --json` first to preview the planned edits to the
repository interface and implementation.

Show the result, then re-run without `--dry-run` to apply if
approved.
