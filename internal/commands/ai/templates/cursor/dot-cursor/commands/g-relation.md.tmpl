# G-relation — wire a GORM association

Run `gofasta g relation <Resource> belongs_to|has_many|has_one
<OtherResource> --dry-run --json` first to preview the model edits
and the foreign-key migration.

Show the result, then re-run without `--dry-run` to apply if
approved.
